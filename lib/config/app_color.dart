import 'package:flutter/material.dart';

class AppColor {
  static final ColorScheme kColor =
      ColorScheme.fromSeed(seedColor: const Color.fromARGB(255, 91, 36, 186));
  static const blackText = Color.fromARGB(197, 0, 0, 0);
  static const buttonColorText = Color.fromARGB(255, 91, 36, 186);
}
